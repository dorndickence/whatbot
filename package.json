{
  "name": "whatbot",
  "version": "1.0.0",
  "description": "personal whatsapp bot",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "heroku-postbuild": "npm run install-system-dependencies && npm run start",
    "install-system-dependencies": "apt-get update && apt-get install -y --no-install-recommends libatk1.0-0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.3.4",
    "chalk": "^5.2.0",
    "dotenv": "^16.0.3",
    "inquirer": "^9.1.5",
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.19.5"
  }
}
